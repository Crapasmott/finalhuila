export const metadata = {
    title: 'Trámites y Servicios | Electrificadora del Huila',
    description: 'Accede a todos los trámites y servicios disponibles para usuarios de la Electrificadora del Huila.',
};

import TramitesUI from './TramitesUI';

export default function TramitesPage() {
    return <TramitesUI />;
}